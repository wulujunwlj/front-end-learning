
Problem1:

	点击选择模式的时候，不会自动打开新的tab，但是现在插件有时候会出现自动打开的效果

	需要修改成 -- 不自动打开tab，但是手动打开新的tab时，显示 hao123[显示网页暂时没问题] (Version1,已废弃)

	需要修改成 -- 选择模式即切换到对应模式，不做其他动作。

Problem2:

	插件点开的时候，应该有一个默认的模式，表现就是前面打勾,但是有时候这个勾不出现。

	需要修改成 -- 显示上一次选择的模式

